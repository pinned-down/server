server:
  port: 9100

spring:
  application:
    name: pinned-down-server

eureka:
  client:
    serviceUrl:
      registerWithEureka: false

de:
  pinneddown:
    server:
      version: @project.version@

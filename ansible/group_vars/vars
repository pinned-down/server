---
system_user: "{{ vault_system_user }}"

github_user: "{{ vault_github_user }}"
github_token: "{{ vault_github_token }}"

registry_url: http://localhost:8761/eureka/

server_secret_key: "{{ vault_server_secret_key }}"
server_region: "{{ vault_server_region }}"
server_ipv4_address: "{{ vault_server_ipv4_address }}"
